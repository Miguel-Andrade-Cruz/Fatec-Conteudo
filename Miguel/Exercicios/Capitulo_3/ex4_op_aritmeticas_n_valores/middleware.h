#include <stdio.h>


void clean_buffer();

float add(int accumulator, int new_number);
float subtract(int accumulator, int new_number);
float multiply(int accumulator, int new_number);
float divide(int accumulator, int new_number);

void show_aritmetics(float acc_sum, float acc_subtraction, float acc_multiplication, float acc_division);